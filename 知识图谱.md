# MiFan 风扇模式 LSPosed 模块：知识图谱与工作总览

> 项目：`com.mifan.kt`（模块）；工作区工程：`xposed-fan-mode-hook/`  
> 目标系统：Xiaomi HyperOS / Android，主要 Hook 目标为 `com.android.systemui`、`com.miui.powerkeeper`、Settings / MiSettings。  
> 本文整理本工作区截至当前的开发、逆向、验证、失败路径与恢复基线。它是项目知识索引，不是 PWM 实施说明。具体用户说明见 `README.md`，回归测试见 `REGRESSION_TESTS.md`，审查记录见 `CODE_REVIEW.md`。

---

## 1. 总体关系图

```text
                    ┌─────────────────────────────────────┐
                    │          com.mifan.kt（LSPosed）      │
                    │          HookEntry.kt                 │
                    └──────────────┬──────────────────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
        ▼                          ▼                          ▼
┌───────────────┐        ┌─────────────────┐        ┌──────────────────┐
│ SystemUI       │        │ PowerKeeper      │        │ Settings/MiSettings│
│ 控制中心二级页 │        │ 官方风扇策略     │        │ 设置页与按应用配置 │
└──────┬────────┘        └────────┬────────┘        └──────────────────┘
       │                           │
       │ fan_mode / 模式条目        │ target_level / 状态机
       ▼                           ▼
┌────────────────────────────────────────────────────────────┐
│ Xiaomi 原厂风扇控制链路                                      │
│ MiCharge HAL → 原厂节点映射 → PowerKeeper / 内核策略          │
└────────────────────────────────────────────────────────────┘
```

### 当前有效的功能边界

- 模块仍以原厂 `fan_mode`、PowerKeeper 的许可逻辑为基础。
- 已保留的扩展模式：
  - **狂暴模式**：值 `4`，使用官方模式语义并在原有策略范围内提高冷却倾向；
  - **自定义模式**：以 `fan_mode=-1` 配合模块偏好项表示，按应用/场景应用既有策略。
- 用户已明确否决“无级 PWM 调节”。当前源码基线不包含 PWM UI、正 PWM 输出、lease 协议或相关 Provider/Receiver。

---

## 2. 当前工作区与恢复基线

### 当前保留目录

```text
/Users/tubi/Desktop/mifan/
├── xposed-fan-mode-hook/       当前模块工程源码
├── backups/                    历史源码快照
├── reverse/                    反编译/逆向资料（必须保留）
├── reverse.zip                 反编译资料压缩备份
├── gradle-8.13/                当前指定构建工具
├── AI_AGENT_WORKFLOW_GUIDE.md  Agent 工作引导
└── 知识图谱.md                 本文
```

### 当前稳定点

当前已验证稳定点：

```text
backups/xposed-fan-mode-hook-src-extreme-navigation-fixed-20260711.tar.gz
```

历史上曾从 PWM phase 1 之前的备份恢复，恢复后确认：

```text
不存在：MODE_PWM、PwmCloseProvider、PwmCloseReceiver、
       ic_fan_mode_extreme、ic_fan_mode_custom、
       decorateInjectedFanModeIcons
```

### 当前构建方式

当前仓库已包含 Gradle wrapper，优先使用项目脚本：

```sh
cd /Users/tubi/Desktop/mifan/xposed-fan-mode-hook
bash scripts/build-debug.sh
```

最近一次确认：

```text
BUILD SUCCESSFUL
```

> 构建仅证明工作区源码可编译；是否安装到真机必须单独确认。恢复源码不会自动覆盖真机已安装模块。

---

## 3. 模块功能知识图谱

### 3.1 Hook 目标

| 目标包 | 主要职责 | 已知 Hook 方向 |
|---|---|---|
| `com.android.systemui` | 控制中心风扇模式二级菜单 | `CoolingFanController`、`CoolingFanDetailAdapter`、模式条目、点击处理 |
| `com.miui.powerkeeper` | 官方风扇策略与最终许可 | `FanStateHandler`、状态读取与模式策略衔接 |
| `com.android.settings` | 常规设置页面 | 风扇模式项和偏好同步 |
| `com.xiaomi.misettings` | 小米设置页面 | 自定义模式、按应用配置、搜索与偏好页兼容 |

模块 Xposed 元数据中曾确认的 scope：

```text
com.android.settings
com.miui.powerkeeper
com.android.systemui
com.xiaomi.misettings
```

### 3.2 SystemUI 模式条目

SystemUI 内部新增条目使用稳定 identity：

| identity | 模式 | 模块值 | 说明 |
|---:|---|---:|---|
| `4` | 狂暴模式 | `4` | 复用官方可识别模式值 |
| `5` | 自定义模式 | `5`（UI identity） | 底层以模块自定义状态维持，常见官方 `fan_mode=-1` |

关键实现流程：

```text
CoolingFanController 构造
  → ensureSystemUiModeItems()
  → 向 _secondaryItems 追加扩展条目

CoolingFanDetailAdapter.updateItems()
  → 确保条目存在
  → 屏蔽 PowerKeeper 短暂的桥接 fan_mode=2 闪烁
  → 由原始模型重新生成二级菜单内容

onDetailItemClick()
  → 识别 identity
  → 写入对应模式/偏好状态
  → 保留原生点击流程或执行模块分支
```

### 3.3 PowerKeeper 与最终许可

观察结论：

```text
target_level > 0
≈ 能观察到的官方最终风扇许可条件
```

已在只读状态观察中确认：

- `FanStateHandler.handleMessage` 与 `FanStateHandler.J("target_level", value)` 是理解原厂状态变化的重要观察点；
- 在当时的 PWM 模式试验状态下，曾捕获 `target_level=1` 与 `officialAllowed=true`；
- 原厂 PowerKeeper/HAL 能在外部 close-only 请求后再次按自身策略写入风扇相关节点。

因此必须区分：

```text
模块请求归零 / 退出模式
≠ 永久禁止原厂风扇策略
```

---

## 4. 反编译与逆向经验

> `reverse/` 是后续维护的核心资料，清理时明确保留。

### 4.1 逆向资料的用途

反编译材料用于回答以下问题：

```text
类名是否存在？
方法和字段在目标 HyperOS 版本中的真实名称是什么？
SystemUI 的详情页由谁持有、谁绘制？
资源名、string、drawable 归属哪个 APK？
PowerKeeper 最终通过哪个状态字段控制风扇？
```

推荐顺序：

```text
1. 从 APK / 反编译目录找类、资源和布局；
2. 在真机通过 LSPosed probe 日志确认类/方法/字段可用；
3. 只 Hook 已验证的目标；
4. 修改后构建、安装、重启对应进程；
5. 从 logcat 验证实际运行路径。
```

### 4.2 SystemUI 资源表与跨 APK 资源限制

最重要的经验：

```text
QSDetailContent.Item.iconRes 是 SystemUI 自身资源表中的整数 ID。
模块 APK 的 R.drawable ID 不能直接填给 SystemUI 的 iconRes。
```

原因：

```text
相同的整数资源 ID 在不同 APK 的 Resources 表中含义不同。
SystemUI 若用自身 Resources 解析模块的 ID，可能得到错误资源、空资源或异常。
```

已确认的原厂相关资源名包括：

```text
ic_qs_coolingfan_mode_performance
ic_qs_coolingfan_mode_silent
ic_qs_coolingfan_mode_selected
ic_qs_coolingfan_enable
quick_settings_coolingfan_mode_high
```

### 4.3 控制中心图标改造尝试与失败结论

#### 原因定位

狂暴模式、自定义模式与“高速强冷”图标重复的直接原因：

```kotlin
context.resourceId("ic_qs_coolingfan_mode_performance", "drawable")
```

扩展模式建模和 fallback 都复用了这个 SystemUI drawable。

#### 已尝试方案

1. 在模块中创建两张 24dp 单色 VectorDrawable：
   - 狂暴：风扇轮廓 + 闪电；
   - 自定义：三条滑杆 + 调节圆点。
2. 在 `createDetailView()` 返回后：
   - 用 `createPackageContext(TARGET_PACKAGE, CONTEXT_IGNORE_SECURITY)` 加载模块 drawable；
   - 遍历 `TextView`，按“狂暴模式”“自定义”定位行；
   - 找到行内第一个 `ImageView` 后调用 `setImageDrawable()`。
3. 加入日志并在真机验证。

#### 真机日志结论

```text
SystemUI fan-mode icon decoration after bind:
extreme=false
custom=false
root=com.android.systemui.qs.QSDetailContent
titles=[]
```

结论：该 HyperOS 版本中，`QSDetailContent` 在 Hook 时是**模型/容器对象**，不是实际承载可见行文本和图标的视图树。即使 `post {}` 延后一次，容器中仍没有标题。

因此下列推断不可靠：

```text
createDetailView() 返回的 QSDetailContent
= 当前实际显示的所有模式行容器
```

后续若重做图标，应优先：

```text
- 反编译追踪 QSDetailContent.Item 的最终 adapter / RecyclerView / 真实 row inflater；
- 或在实际 row bind / setItem / onBindViewHolder 类似位置 Hook；
- 先打印真实绘制容器的 class、id、child tree，再修改；
- 不要再只从空 QSDetailContent 向上猜父层级。
```

该图标尝试的源码已回滚，不在当前基线内。

### 4.4 反射与 Hook 稳定性经验

- 不同 HyperOS 版本的字段、方法可能混淆或变动；所有反射目标都应在安装 Hook 前 probe。
- 使用 `runCatching` / `onFailure` 记录失败，避免一个非关键 UI Hook 使 SystemUI 崩溃。
- 针对集合字段（例如 `_secondaryItems`），只追加自己的 identity，不改写官方 identity。
- 对模式中转值需谨慎：PowerKeeper 短时 `fan_mode=2` 可能只是退出路径桥接状态，直接呈现在 UI 会导致模式闪跳。
- 每次重装模块后通常需重启对应进程；SystemUI 验证可杀掉 `com.android.systemui`，由系统拉起新进程。

---

## 5. MiCharge HAL 与 PWM 研究历史（已冻结/不继续）

> 本节记录历史结论，防止未来重复踩坑。用户当前不要求无级 PWM，禁止自动恢复这些功能。

### 5.1 研究目标与结论

曾尝试通过 Xiaomi MiCharge HAL 查找 `pwm_duty` 类路径或官方控制接口。

结论：原厂 HAL node map 不支持该路径。以下操作有明显风险：

```text
字符串替换
node map 注入
code cave
vtable patch
overlay / 修改 vendor HAL
```

风险包括：

```text
/vendor/bin/hw/vendor.xiaomi.hardware.micharge-service 崩溃
AIDL 服务异常
HAL 健康损失
```

**长期约束：不得 patch、overlay 或改造 MiCharge HAL。**

已知正常 HAL SHA-256：

```text
b9a9e06c4dffd74e6d27b5062ce22fae4ee950175351043d184ed885d9c182a3
```

### 5.2 曾部署的 close-only helper（历史）

历史上曾存在 root helper 和 close-only 退出桥：

```text
SystemUI
  → ContentResolver.call()
  → PwmCloseProvider
  → root helper FIFO
  → SET <generation> 0 0
  → pwm_duty=0
```

之所以从 receiver 改为 Provider，是因为 HyperOS Greezer 拒绝了 SystemUI 指向模块 receiver 的显式广播：

```text
BroadcastQueue: Greezer Denial
```

加固后 helper 只接受：

```text
SET n 0 0
STATUS
```

历史 hardened helper SHA-256：

```text
4afb17422d448d719ada04e737bd3e972ef2d4588bc032c9cd20e54ee8f49f7e
```

该闭环曾回归成功，但它不意味着永久禁止原厂风扇策略。相关工作区文档和实验目录已经清理；真机上是否仍残留 helper 应仅在用户明确要求清理时单独处理。

### 5.3 为什么没有继续正 PWM

主要阻断因素：

1. 无法证明调用方具备独占、可信的正 PWM 写入资格；
2. 原厂 PowerKeeper/HAL 可能同时写同一节点；
3. 模块、Settings、SystemUI、root helper 之间缺少可证明的安全授权根；
4. 修改原厂 HAL 风险不可接受；
5. 用户最终明确要求停止无级 PWM 并恢复此前源码。

因此当前原则为：

```text
不实现、不测试、不部署正 duty。
不恢复 PwmCloseProvider / PwmCloseReceiver。
不恢复 GRANT / RENEW / lease / capability 协议。
不读取或写入 PWM sysfs 节点，除非用户重新明确授权并单独立项。
```

---

## 6. 备份与恢复策略

### 6.1 工程 Git 状态

当前工程已接入 GitHub：

```text
https://github.com/Yunnijian/Xiaomi_FanUltra.git
```

Git 历史可用于普通 diff/revert，但在大范围修改、Hook 逻辑试验、安装测试前仍建议保留 `backups/` 下的 tar/zip 快照，方便快速恢复到真机验证过的稳定点。

### 6.2 安全操作流程

任何大范围修改前：

```sh
cd /Users/tubi/Desktop/mifan
# 示例：对工程源码做独立快照
tar -czf backups/xposed-fan-mode-hook-src-before-<change>-<timestamp>.tar.gz \
  xposed-fan-mode-hook/app/src \
  xposed-fan-mode-hook/settings.gradle.kts \
  xposed-fan-mode-hook/build.gradle.kts \
  xposed-fan-mode-hook/gradle.properties
```

恢复时建议：

```text
1. 先为当前源码创建快照；
2. 解压到临时目录；
3. 查看 archive 内的顶级结构；
4. 仅覆盖明确需要恢复的 app/src 与项目配置；
5. 删除备份中不存在、但当前改动引入的遗留源码文件；
6. 用指定 Gradle 编译验证；
7. 明确区分“源码已恢复”和“真机 APK 已覆盖安装”。
```

---

## 7. 真机部署与日志方法

### 7.1 安装与重启 SystemUI

```sh
cd /Users/tubi/Desktop/mifan/xposed-fan-mode-hook
bash scripts/build-debug.sh
/Users/tubi/Desktop/mifan/.toolchains/android-sdk/platform-tools/adb -s 75bbaf40 install -r app/build/outputs/apk/debug/app-debug.apk

# 仅在用户明确要求时重启相关作用域；默认安装后由用户手动重启。
```

注意：

- `pm install -r` 成功不代表 LSPosed 对现有进程已重新加载新 dex；
- 重启 SystemUI 后才可验证 SystemUI scope 的新 Hook，但当前协作约定是安装后不自动重启作用域；
- PowerKeeper 或 Settings 的变动需要分别重启相应进程或系统按需重建，由用户手动执行；
- 安装前后可用 `dumpsys package com.mifan.kt` 检查 `codePath` 与 `lastUpdateTime`。

### 7.2 有效日志过滤

```sh
logcat -d -v threadtime \
  | grep 'LSPosedFramework' \
  | grep '(com.android.systemui)' \
  | grep 'FanModeHook'
```

重点验证：

```text
SystemUI cooling fan probe
CoolingFanTile hooks installed
SystemUI cooling fan extra mode items appended
SystemUI cooling fan detail items rebuilt
SystemUI bridge fan mode masked
```

避免把 Operit 自己的对话/工具日志误认为模块日志；优先筛选：

```text
LSPosedFramework + 目标进程 + [com.mifan.kt,FanModeHook]
```

---

## 8. 已知问题、限制与后续建议

### 已知问题

1. 狂暴模式、自定义模式图标仍与高速强冷复用，因当前基线已回滚图标试验。
2. 不能将模块 drawable ID 直接放到 SystemUI 的 `iconRes`。
3. 尚未定位本 ROM 中控制中心二级页实际 row 的最终绑定位置。
4. 当前真机可能仍装有图标试验版或历史 PWM close-only 版模块；工作区源码状态不自动代表设备状态。

### 建议的后续路线（仅在用户重新提出图标需求时）

```text
反编译 SystemUI：
  QSDetailContent
    → Item model 的消费点
    → 实际 adapter / row inflater
    → icon ImageView 的 bind 方法

真机日志：
  在 row bind 前后打印 item identity、title、ImageView class/id

实现：
  仅对 identity=4 / identity=5 设置模块 drawable
  不通过 title 文本猜测
  不影响 selected/checkmark 子视图

验证：
  打开菜单、切换模式、关闭重开、SystemUI 重启后均检查
```

### 不应做的事情

```text
- 不 patch MiCharge HAL；
- 不为了改变图标把模块资源 ID 写入 SystemUI iconRes；
- 不重新引入 PWM 无级调节或正 duty；
- 不删除 reverse/ 反编译目录；
- 不在无备份情况下大范围覆盖 HookEntry.kt；
- 不将“源码恢复”误说成“真机已恢复”。
```

---

## 9. 一页速查

```text
当前稳定备份：xposed-fan-mode-hook-src-extreme-navigation-fixed-20260711.tar.gz
当前工程：xposed-fan-mode-hook/
构建命令：bash scripts/build-debug.sh
逆向资料：reverse/（保留）
HAL：禁止修改
PWM：用户已否决，不继续
SystemUI 图标：曾尝试，已回滚；根因是跨 APK resource ID 与真实 row 容器未定位
GitHub：Yunnijian/Xiaomi_FanUltra.git
```

---

## 10. Smali 逆向索引附录

本节合并自原 `smali-analysis.md`，保留 HookEntry 维护最常用的逆向索引。完整反编译材料仍保留在工作区 `reverse/` 目录。

### 10.1 Settings 侧

| Smali | 作用 |
|---|---|
| `FanModeController.smali` | 风扇模式控制器；模式 entry/value、`fan_mode` 写入来源 |
| `FanModeController$FanModeContentObserver` | 监听 `fan_mode` 与 `cooling_fan_enable` 变化 |
| `CoolingFanEnableController.smali` | 散热风扇总开关 |
| `FanModeRangeController.smali` | 使用范围，全场景/部分场景 |
| `FanSceneGameController.smali` | 游戏场景开关 |
| `FanSceneRapidChargeController.smali` | 快充场景开关 |
| `FanSmartStopOnRecordingController.smali` | 录音智能停转开关 |
| `MiuiCoolingFanSettings.smali` | 散热风扇设置 Fragment |

### 10.2 PowerKeeper 侧

核心类：`FanStateHandler.smali`。

| 字段 | 类型 | HookEntry 含义 |
|---|---|---|
| `g` | `ContentResolver` | 主 resolver |
| `h` | `ContentResolver` | 备用 resolver |
| `l` | `int` | `fan_mode_range` |
| `p` | `int` | 当前 `target_level` |
| `w` | `String` | 当前 thermal 场景 |
| `x` | `String` | 前台应用包名 |
| `y` | `boolean` | 游戏场景开关 |
| `z` | `boolean` | 快充场景开关 |
| `A` | `boolean` | 导航场景开关 |
| `B` | `boolean` | 录音智能停转开关 |
| `C` | `boolean` | 快充活跃状态 |
| `D` | `boolean` | 导航场景状态 |
| `F` | `boolean` | 录音活跃状态 |
| `L` | `boolean` | 锁屏/息屏状态 |
| `M` | `boolean` | SIM 通话状态 |
| `N` | `boolean` | 听筒状态 |
| `m0` | `Map` | 导航 App 白名单/场景映射 |

关键方法：

| 方法 | 作用 |
|---|---|
| `J(String, String)` | 写入 PowerKeeper 路径，当前用于 `target_level` |
| `W(FanStateHandler$g)` | 官方状态快照处理，息屏保持逻辑在这里屏蔽锁屏分支 |
| `handleMessage(Message)` | 设置/前台/设备状态消息入口，模块在原逻辑后补写策略 |
| `o()` | 初始化/重读系统设置，模块在原逻辑后补写策略 |

### 10.3 SystemUI 侧

| Smali | 作用 |
|---|---|
| `CoolingFanController.smali` | 控制中心风扇控制器，维护二级页模式条目 |
| `CoolingFanTile.smali` | 控制中心磁贴入口 |
| `CoolingFanTile$CoolingFanDetailAdapter.smali` | 二级页适配器与点击处理 |

### 10.4 MISettings 侧

| Smali | 作用 |
|---|---|
| `HighRefreshOptionsActivity.smali` | 借用高刷配置 Activity 作为按应用风扇配置宿主 |
| `HighRefreshOptionsFragment.smali` | 列表 Fragment，模块替换数据源 |
| `AppSearchFragment.smali` | 搜索页兼容 |
| `oa.h` | App item model |
| `oa.j` | 行 ViewHolder，ROM 差异较大，只作为可选增强 |

### 10.5 与 HookEntry.kt 的对应关系

| HookEntry.kt 常量 | Smali 来源 |
|---|---|
| `FAN_MODE_CONTROLLER` | Settings `FanModeController.smali` |
| `FAN_STATE_HANDLER` | PowerKeeper `FanStateHandler.smali` |
| `SYSTEMUI_COOLING_FAN_CONTROLLER` | SystemUI `CoolingFanController.smali` |
| `SYSTEMUI_COOLING_FAN_DETAIL_ADAPTER` | SystemUI `CoolingFanTile$CoolingFanDetailAdapter.smali` |
| `COOLING_FAN_ENABLE_CONTROLLER` | Settings `CoolingFanEnableController.smali` |
| `MISETTINGS_HIGH_REFRESH_OPTIONS_ACTIVITY` | MISettings `HighRefreshOptionsActivity.smali` |
| `MISETTINGS_HIGH_REFRESH_OPTIONS_FRAGMENT` | MISettings `HighRefreshOptionsFragment.smali` |
| `MISETTINGS_FOLLOW_VIEW_HOLDER` | MISettings `oa.j` |
| `MISETTINGS_APP_ITEM_MODEL` | MISettings `oa.h` |
